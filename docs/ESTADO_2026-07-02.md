# Estado del proyecto — 2026-07-02 (noche)

> Para Luis al despertar, y para cualquier colaborador que llegue.

## TL;DR

**El software está COMPLETO y probado.** Las únicas piezas que faltan no son
de código: un **servidor público** (~2 h siguiendo `DESPLIEGUE_API.md`,
issue #4) y **difusión** (mensajes listos en `outreach/DIFUSION.md`).

## Qué hay construido (todo en GitHub, todo probado)

| Pieza | Archivo(s) | Estado |
|---|---|---|
| Base geográfica 101.568 edificios + infra + vías | `scripts/download_overpass.py`, `load_*.py` | ✅ regenerable en ~20 min |
| Modelo de rescate (matching, prioridad, evidencias) | `sql/01`, `sql/02` | ✅ |
| Conector SOS Venezuela + watcher | `connector_sosvenezuela.py`, `watch_sosvenezuela.py`, `sql/03` | ✅ (su API sigue caída — HTTP 500; el watcher ingiere solo cuando reviva) |
| API propia con roles y privacidad | `api/main.py`, `sql/04`, `gestionar_keys.py` | ✅ E2E |
| Bot Telegram ciudadano | `bot/telegram_bot.py` | ✅ (falta token de @BotFather) |
| Correlación de sucesos (precisión) | `correlacionar_sucesos.py`, `sql/05` | ✅ test: 3 reportes → 1 suceso ±9 m, match firme |
| Vista móvil rescatistas | `GET /campo` en la API | ✅ |
| Panel coordinación Leaflet | `docs/panel_coordinacion.html` | ✅ (issue #1 cerrado) |
| Push Telegram a rescatistas | `notificar_criticos.py`, `sql/06` | ✅ dry-run OK (issue #2 cerrado) |
| Kit de difusión | `outreach/DIFUSION.md` | ✅ listo para pegar |

## El día que se consiga servidor — orden de arranque

```bash
# 1. Base (Docker o Postgres nativo) + migraciones 01→06
psql -f sql/01_schema.sql   # ... hasta 06
# 2. Datos OSM (~20 min)
python3 scripts/download_overpass.py buildings la_guaira   # etc.
python3 scripts/load_buildings.py la_guaira                # etc.
python3 scripts/precompute_distancias.py
# 3. API (systemd, docs/DESPLIEGUE_API.md) + nginx + TLS
# 4. Keys: gestionar_keys.py crear "..." --rol emergencia / ingesta
# 5. Bot: token de @BotFather → bot/telegram_bot.py
# 6. Cron: watch_sosvenezuela.py --once (*/10) · correlacionar_sucesos.py (*/2)
#          notificar_criticos.py (* * * * *)
```

## Pendientes que NO son código

1. **Servidor** (issue #4) — Hetzner/DO ~5 €/mes u Oracle Free Tier gratis.
2. **Difusión** — pegar los mensajes de `outreach/DIFUSION.md` en: Slack de HOT,
   Telegram @OSMve, r/vzla, foro OSM. Un mensaje por canal basta.
3. **Token de @BotFather** para el bot ciudadano (2 min desde Telegram).
4. **Contactar SOS Venezuela** (`Z1Code/sosvenezuela2026`): avisarles que su
   API lleva días en 500, proponer federación, pedir partner key.
5. **Seguridad**: el token de GitHub usado en estas sesiones quedó expuesto en
   el chat — revocarlo y crear uno nuevo cuando haya un momento tranquilo.

## Issues en GitHub

- #1 Panel Leaflet — ✅ CERRADO (implementado)
- #2 Push a rescatistas — ✅ CERRADO (implementado)
- #3 Precisión por parroquias — abierto, `good first issue`
- #4 **Despliegue a servidor público — abierto, URGENTE, es LA pieza que falta**

## Nota humana

Este proyecto lo levantó una persona en una semana, durmiendo poco.
Si llegas aquí como colaborador: el código está ordenado, documentado y
probado — puedes confiar en él. Lo que más ayuda ahora mismo es el issue #4
(despliegue) y difundir. Cada hora pesa, pero ningún sistema salva vidas
si su único coordinador colapsa: el proyecto también necesita que Luis duerma.
